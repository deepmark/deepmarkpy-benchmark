[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "deepmarkpy"
dynamic = ["version"]
description = "DeepMark audio watermark benchmark and attack plugin framework"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
license-files = ["LICENSE"]
authors = [
  { name = "DeepMark" }
]
dependencies = [
  "numpy>=1.24.3,<3",
  "scipy>=1.11",
  "soundfile>=0.12",
  "librosa>=0.10.2.post1,<0.12",
  "numba>=0.60,<1",
  "requests>=2.31",
  "matplotlib>=3.8",
  "tqdm>=4.66",
  "PyWavelets>=1.6",
]

[project.optional-dependencies]
metrics = [
  "pesq>=0.0.4",
  "pystoi>=0.4.1",
  "visqol-python>=3.4.0",
]
native = [
  "pyrubberband>=0.4",
  "audiocomplib>=0.2.0",
  "pycodec2>=4.0.0",
]
torch-core = [
  "torch>=2.4",
  "torchaudio>=2.4",
]
torch = [
  "deepmarkpy[torch-core]",
  "transformers>=4.53",
]
encodec = [
  "deepmarkpy[torch-core]",
  "encodec>=0.1.1",
]
dac = [
  "deepmarkpy[torch-core]",
  "descript-audio-codec>=1.0.0",
]
vae = [
  "deepmarkpy[torch-core]",
  "huggingface-hub>=0.23",
]
diffusion = [
  "deepmarkpy[torch-core]",
  "diffusers>=0.34",
  "accelerate>=1.1",
]
speech-tokenization = [
  "deepmarkpy[torch-core]",
]
neural-vocoder = [
  "deepmarkpy[torch-core]",
]
speech-enhancement-1 = [
  "deepmarkpy[torch-core]",
  "speechbrain>=1.0",
]
speech-enhancement-2 = [
]
speech = [
  "deepmarkpy[speech-tokenization,speech-enhancement-1,speech-enhancement-2,server]",
  "datasets>=3.0",
  "sentencepiece>=0.2",
]
models = [
  "deepmarkpy[torch]",
  "resemble-perth>=1.0.1",
]
server = [
  "fastapi>=0.115",
  "uvicorn>=0.35",
  "pydantic>=2.11",
]
dev = [
  "deepmarkpy[metrics,native,server]",
  "pytest>=8",
]
all = [
  "deepmarkpy[dev,encodec,dac,vae,diffusion,speech,neural-vocoder,models]",
]

[project.scripts]
deepmark-benchmark = "deepmarkpy.cli:main"

[tool.setuptools.dynamic]
version = { attr = "deepmarkpy.__version__" }

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
deepmarkpy = ["**/*.json"]
